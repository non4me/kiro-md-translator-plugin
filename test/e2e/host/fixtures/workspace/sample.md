# Sample document

A paragraph with **bold**, _italic_ and an `inline code` span, plus a
[link](https://example.com/) that must never be followed.

## Lists

- first item
- second item with `code`

1. ordered one
2. ordered two

## A GFM table

| Column A | Column B |
| -------- | -------- |
| a1       | b1       |
| a2       | b2       |

## A fenced code block

```ts
export const answer: number = 42
```

## A relative image

![a tiny placeholder](./img.png)

> A block quote, so the renderer sees one more node kind.
